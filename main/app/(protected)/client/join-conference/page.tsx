import { JoinConference } from "@/components/conference/join-conference";

const page = () => {
  return (
    <div className="relative h-full w-full">
      <JoinConference />
    </div>
  );
};

export default page;
